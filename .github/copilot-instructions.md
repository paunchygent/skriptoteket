# Repository instructions for Copilot

Follow `AGENTS.md`.

Documentation contract:
- MUST use the package-owned `repository-governance` document contract
- MUST use the package-owned scaffold commands for new governed documents
- MUST treat `docs/_meta/docs-contract.yaml` as historical-only

Before proposing or committing docs changes:
- Run: `pdm run docs-validate`
