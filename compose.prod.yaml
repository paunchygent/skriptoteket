# Production deployment for home server (hemma.hule.education)
# Usage: docker compose -f compose.prod.yaml build && docker compose -f compose.prod.yaml up -d
#
# Prerequisites:
# - External network: docker network create hule-network
# - External PostgreSQL: shared-postgres on hule-network
# - Environment variables in .env (see .env.example.prod)

name: skriptoteket

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: skriptoteket-web:latest
    container_name: skriptoteket-web
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DATABASE_URL: postgresql+asyncpg://skriptoteket:${SKRIPTOTEKET_DB_PASSWORD}@shared-postgres:5432/skriptoteket
      SECRET_KEY: ${SECRET_KEY}
      ENVIRONMENT: production
      COOKIE_SECURE: "true"
      RUNNER_IMAGE: skriptoteket-runner:latest
      ARTIFACTS_ROOT: /app/.artifacts
      # Observability (requires compose.observability.yaml running)
      OTEL_TRACING_ENABLED: "true"
      OTEL_EXPORTER_OTLP_ENDPOINT: http://jaeger:4317
      SERVICE_VERSION: "0.1.0"
      # Email (SMTP)
      EMAIL_PROVIDER: ${EMAIL_PROVIDER:-smtp}
      EMAIL_SMTP_HOST: ${EMAIL_SMTP_HOST:-mail.privateemail.com}
      EMAIL_SMTP_PORT: ${EMAIL_SMTP_PORT:-587}
      EMAIL_SMTP_USERNAME: ${EMAIL_SMTP_USERNAME:-}
      EMAIL_SMTP_PASSWORD: ${EMAIL_SMTP_PASSWORD:-}
      EMAIL_SMTP_USE_TLS: ${EMAIL_SMTP_USE_TLS:-true}
      EMAIL_SMTP_TIMEOUT: ${EMAIL_SMTP_TIMEOUT:-30}
      EMAIL_DEFAULT_FROM_EMAIL: ${EMAIL_DEFAULT_FROM_EMAIL:-noreply@hule.education}
      EMAIL_DEFAULT_FROM_NAME: ${EMAIL_DEFAULT_FROM_NAME:-Skriptoteket}
      HEALTHZ_SMTP_CHECK_ENABLED: ${HEALTHZ_SMTP_CHECK_ENABLED:-true}
      EMAIL_VERIFICATION_BASE_URL: ${EMAIL_VERIFICATION_BASE_URL:-https://skriptoteket.hule.education}
      EMAIL_VERIFICATION_TTL_HOURS: ${EMAIL_VERIFICATION_TTL_HOURS:-24}
      # LLM API (host.docker.internal reaches host's llama-server)
      LLM_EDIT_ENABLED: ${LLM_EDIT_ENABLED:-false}
      LLM_EDIT_BASE_URL: ${LLM_EDIT_BASE_URL:-http://host.docker.internal:8082}
      OPENAI_LLM_EDIT_API_KEY: ${OPENAI_LLM_EDIT_API_KEY:-}
      LLM_EDIT_MODEL: ${LLM_EDIT_MODEL:-qwen3-coder-30b-a3b}
      LLM_EDIT_TEMPLATE_ID: ${LLM_EDIT_TEMPLATE_ID:-edit_suggestion_v1}
      LLM_EDIT_MAX_TOKENS: ${LLM_EDIT_MAX_TOKENS:-512}
      LLM_EDIT_TEMPERATURE: ${LLM_EDIT_TEMPERATURE:-0.2}
      LLM_EDIT_TIMEOUT_SECONDS: ${LLM_EDIT_TIMEOUT_SECONDS:-60}
      LLM_EDIT_CONTEXT_WINDOW_TOKENS: ${LLM_EDIT_CONTEXT_WINDOW_TOKENS:-4096}
      LLM_EDIT_CONTEXT_SAFETY_MARGIN_TOKENS: ${LLM_EDIT_CONTEXT_SAFETY_MARGIN_TOKENS:-256}
      LLM_EDIT_SYSTEM_PROMPT_MAX_TOKENS: ${LLM_EDIT_SYSTEM_PROMPT_MAX_TOKENS:-1024}
      LLM_EDIT_INSTRUCTION_MAX_TOKENS: ${LLM_EDIT_INSTRUCTION_MAX_TOKENS:-128}
      LLM_EDIT_SELECTION_MAX_TOKENS: ${LLM_EDIT_SELECTION_MAX_TOKENS:-896}
      LLM_EDIT_PREFIX_MAX_TOKENS: ${LLM_EDIT_PREFIX_MAX_TOKENS:-1024}
      LLM_EDIT_SUFFIX_MAX_TOKENS: ${LLM_EDIT_SUFFIX_MAX_TOKENS:-256}
      LLM_EDIT_PROMPT_CACHE_RETENTION: ${LLM_EDIT_PROMPT_CACHE_RETENTION:-}
      LLM_EDIT_PROMPT_CACHE_KEY: ${LLM_EDIT_PROMPT_CACHE_KEY:-}
      LLM_EDIT_EXTRA_HEADERS: ${LLM_EDIT_EXTRA_HEADERS:-}
      LLM_COMPLETION_ENABLED: ${LLM_COMPLETION_ENABLED:-false}
      LLM_COMPLETION_BASE_URL: ${LLM_COMPLETION_BASE_URL:-http://host.docker.internal:8082}
      OPENAI_LLM_COMPLETION_API_KEY: ${OPENAI_LLM_COMPLETION_API_KEY:-}
      LLM_COMPLETION_MODEL: ${LLM_COMPLETION_MODEL:-qwen3-coder-30b-a3b}
      LLM_COMPLETION_TEMPLATE_ID: ${LLM_COMPLETION_TEMPLATE_ID:-inline_completion_v1}
      LLM_COMPLETION_PROMPT_CACHE_RETENTION: ${LLM_COMPLETION_PROMPT_CACHE_RETENTION:-}
      LLM_COMPLETION_PROMPT_CACHE_KEY: ${LLM_COMPLETION_PROMPT_CACHE_KEY:-}
      LLM_COMPLETION_EXTRA_HEADERS: ${LLM_COMPLETION_EXTRA_HEADERS:-}
      LLM_CHAT_ENABLED: ${LLM_CHAT_ENABLED:-false}
      LLM_CHAT_BASE_URL: ${LLM_CHAT_BASE_URL:-http://host.docker.internal:8082}
      OPENAI_LLM_CHAT_API_KEY: ${OPENAI_LLM_CHAT_API_KEY:-}
      LLM_CHAT_MODEL: ${LLM_CHAT_MODEL:-qwen3-coder-30b-a3b}
      LLM_CHAT_TEMPLATE_ID: ${LLM_CHAT_TEMPLATE_ID:-editor_chat_v1}
      LLM_CHAT_PROMPT_CACHE_RETENTION: ${LLM_CHAT_PROMPT_CACHE_RETENTION:-}
      LLM_CHAT_PROMPT_CACHE_KEY: ${LLM_CHAT_PROMPT_CACHE_KEY:-}
      LLM_CHAT_EXTRA_HEADERS: ${LLM_CHAT_EXTRA_HEADERS:-}
      LLM_CHAT_OPS_ENABLED: ${LLM_CHAT_OPS_ENABLED:-false}
      LLM_CHAT_OPS_BASE_URL: ${LLM_CHAT_OPS_BASE_URL:-http://host.docker.internal:8082}
      OPENAI_LLM_CHAT_OPS_API_KEY: ${OPENAI_LLM_CHAT_OPS_API_KEY:-}
      LLM_CHAT_OPS_MODEL: ${LLM_CHAT_OPS_MODEL:-qwen3-coder-30b-a3b}
      LLM_CHAT_OPS_TEMPLATE_ID: ${LLM_CHAT_OPS_TEMPLATE_ID:-editor_chat_ops_v1}
      LLM_CHAT_OPS_MAX_TOKENS: ${LLM_CHAT_OPS_MAX_TOKENS:-1500}
      LLM_CHAT_OPS_TEMPERATURE: ${LLM_CHAT_OPS_TEMPERATURE:-0.2}
      LLM_CHAT_OPS_TIMEOUT_SECONDS: ${LLM_CHAT_OPS_TIMEOUT_SECONDS:-60}
      LLM_CHAT_OPS_CONTEXT_WINDOW_TOKENS: ${LLM_CHAT_OPS_CONTEXT_WINDOW_TOKENS:-16384}
      LLM_CHAT_OPS_CONTEXT_SAFETY_MARGIN_TOKENS: ${LLM_CHAT_OPS_CONTEXT_SAFETY_MARGIN_TOKENS:-256}
      LLM_CHAT_OPS_SYSTEM_PROMPT_MAX_TOKENS: ${LLM_CHAT_OPS_SYSTEM_PROMPT_MAX_TOKENS:-1024}
      LLM_CHAT_OPS_PROMPT_CACHE_RETENTION: ${LLM_CHAT_OPS_PROMPT_CACHE_RETENTION:-}
      LLM_CHAT_OPS_PROMPT_CACHE_KEY: ${LLM_CHAT_OPS_PROMPT_CACHE_KEY:-}
      LLM_CHAT_OPS_EXTRA_HEADERS: ${LLM_CHAT_OPS_EXTRA_HEADERS:-}
      # nginx-proxy integration
      VIRTUAL_HOST: skriptoteket.hule.education
      VIRTUAL_PORT: "8000"
      LETSENCRYPT_HOST: skriptoteket.hule.education
    expose:
      - "8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - artifacts_data:/app/.artifacts
    networks:
      - hule-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Runner image - built but not run as a service
  # Web app starts runner containers dynamically via Docker API
  runner:
    build:
      context: .
      dockerfile: Dockerfile.runner
    image: skriptoteket-runner:latest
    profiles:
      - build-only

networks:
  hule-network:
    external: true

volumes:
  artifacts_data:
