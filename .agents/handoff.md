# Session Handoff
Keep this file updated so the next session can pick up work quickly.
## Editing Rules (do not break structure)
- Keep the headings and section order exactly as-is; only fill in content.
- Use short bullets; include key file paths (e.g., `src/...`, `docs/...`) and exact commands.
- Do not paste large code blocks; link to files instead.
- Never include secrets/tokens/passwords or personal data.
- Keep this file under 200 lines; move history to `docs/`.
## Snapshot
- Date: 2026-04-04
- Branch: `main` + local changes
- Current sprint: Sprint 24
- Production: Full Vue SPA
- Completed: Recent PRs through PR-0214
## Status
- `ST-25-05` / `PR-0192` is now migrated onto the compiled pinball-table system locally: `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/table/prototypeAlphaTableSpec.ts` is the authored source of truth, `pinballTablePlanTypes.ts` and `compilePinballTable.ts` now produce the compiled physics/render plan, `prototypeAlphaTable.ts` exports that compiled table directly, `PhysicsWorld.ts` now builds the Rapier world from compiled bodies/colliders instead of the removed `createLaneDevices.ts` / `createTargetDevices.ts` path, and `staticBoardUnderlay.ts` now renders compiled render nodes instead of the removed authored-table primitives. The old authored-table stack (`authoredTableTypes.ts`, `boardGeometryPrimitives.ts`, `prototypeAlphaAuthoredTable.ts`, `compileAuthoredTable.ts`) is deleted. Post-review fixes are included: default serve spawns now honor compiled `launchVelocity`, and the compiler now rejects duplicate semantic device tags.
- `PR-0193` planning scope is now explicitly checkpointed in docs as `PR-0193a`/`PR-0193b`/`PR-0193c` inside the single PR task (`docs/backlog/prs/pr-0193-flunk-out-frenzy-capture-eject-and-save-devices.md`): checkpoint A locks capture/save contracts + compiler validation, checkpoint B locks deterministic capture/hold/eject/save physics lifecycle emission, and checkpoint C locks rules/effects/reset integration with final integrated proof.
- `ST-25-05` / `PR-0193` implementation is now in place locally across all checkpoints: checkpoint A contract/compiler wiring is active in `tableDefinitionTypes.ts`, `pinballTablePlanTypes.ts`, `compilePinballTable.ts`, and `prototypeAlphaTableSpec.ts`; checkpoint B capture/save lifecycle is active in `captureDeviceLifecycle.ts` + `PhysicsWorld.ts`; and checkpoint C rule/effect wiring is active in `captureAwardsState.ts`, `RuleEngine.ts`, `ruleTypes.ts`, `gameEffectTypes.ts`, `PrototypeAlphaGameEngine.ts`, `PixiRenderer.ts`, and `AudioDirector.ts`. The long-standing flipper contact fixture drift is also corrected in `flipperContactModel.spec.ts` so the full physics+rules+engine matrix now passes in one run.
- Scope note for the donor-review follow-up: the local worktree already contains separate `PR-0193` capture/save WIP in shared table files such as `tableDefinitionTypes.ts`, `compilePinballTable.ts`, and `prototypeAlphaTableSpec.ts`; this `PR-0198` follow-up only changes the donor-review hunks inside those files and does not extend the capture/save slice.
- `ST-25-06` is now split explicitly across two linked donor-fidelity tasks: `PR-0198` owns the VPW donor topology and board-carrier cutover, while new `PR-0199` owns full donor semantic representation for richer triggers, gates, and rollover shapes. The donor map reference (`data/pinball_resources_and_repos/vpw-rom-example-table-donor-map.md`) and the pinball authoring skill (`~/.codex/skills/pinball-board-authoring/SKILL.md`) now state the hard rule: if a donor object exceeds the current schema, extend the schema/compiler/runtime instead of flattening it into a simpler local shape or undocumented remap.
- `ST-25-06` donor-lane governance is now stricter: donor lanes and launcher corridors must not rely on flattened `laneBounds` / AABB containment seams when the donor defines shaped lane regions. `PR-0199` now explicitly owns replacing those remaining seams with donor-shaped lane-region semantics across the launcher corridor and related board lanes.
- `ST-25-06` launcher corrective lane is no longer allowed to continue straight through `PR-0200`/`PR-0202`/`PR-0203` on the old route-driven assumptions. Lower shooter ownership stays donor-authored (`Wall95`, `Wall34`, `Wall010`, `Wall011`, `Apron1`, `Apron2`) with the thin `Wall263` shoulder splice, but further elevated cut-over continuation is now blocked on active `EPIC-33` / `ST-33-01` so carrier-role schema, launcher-world ownership, donor overhead collider foundation, and observer/cut-over governance land first.
- `PR-0200` / `PR-0202` / `PR-0203` are now planning-blocked for further continuation by the architect direction in `docs/reference/ref-flunk-out-frenzy-physical-rail-architect-direction-2026-04-04.md`: `travelRoutes` must become observation/proof spines, physical motion must move to a carrier graph, and no further cut-over work should proceed until the new foundation lane exists.
- `PR-0206` is now staged as a bounded proof-first lane for the unresolved inert-launch defect: deterministic red launch-effect tests + explicit launcher telemetry contract + retained matrix artifact schema, with production geometry/runtime behavior fixes explicitly blocked until independent review approval.
- `PR-0206` is now implemented as a proof-only lane (no geometry/runtime behavior fix): launcher telemetry contract is wired end-to-end (`launcherChain3d.ts` -> `PhysicsWorld.ts` -> `PrototypeAlphaGameEngine.ts` -> `GameRuntime.ts` -> `GameHost.vue` debug seam), and a deterministic full-charge launch-effect proof test is now intentionally red in `PhysicsWorld.spec.ts`.
- `PR-0207` is now approved and implemented as a proof-first gate only: focused test/telemetry contract tightening in `compilePinballTable.spec.ts`, `PhysicsWorld.spec.ts`, and `GameRuntime.spec.ts` locks unchanged PR-0206 matrix parameters plus endpoint-bridge seam-contract assertions, while production geometry/runtime behavior fixes remain explicitly blocked.
- `PR-0208` runtime seam changes remain in place and the focused `PR-0209` matrix proof is now green locally, including relaunch case `K-RELAUNCH-MEDIUM` with observed `sw16` exit in deterministic trace artifacts.
- `PR-0209` remains the active hardening gate for launch-to-drop observability: matrix artifacts now persist from focused tests even when invariants fail, and failing case IDs are emitted explicitly by the proof assertion path.
- `PR-0212` is now approved as the explicit breach-inventory/truth-gate audit slice (`docs/backlog/prs/pr-0212-flunk-out-frenzy-launcher-shortcut-breach-inventory-and-truth-gate-audit.md`), `PR-0213` is closed as done, and `PR-0214` is now implemented as the canonical launch-trace operator surface: `scripts/playwright_flunk_out_frenzy_launch_trace_parity_check.py` emits the raw trace matrix plus `launch-to-drop-trace-summary.json` and `launch-to-drop-trace-summary.md`, both derived by `scripts/_flunk_out_frenzy_launch_trace_summary.py` against the pinned focused baseline at `frontend/apps/skriptoteket/.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`.
- `PR-0215` is now tightened to a bounded runtime-honesty checkpoint, not the physical-carrier cut-over vehicle: it still owns speed-fabrication exposure, remaining transport debt visibility, and strict truth-gate retention, but carrier-role schema, launcher-world ownership, donor overhead collider foundation, and future cut-over governance now belong to active `EPIC-33` / `ST-33-01` and planned `PR-0217` / `PR-0218` / `PR-0219`.
- `PR-0215` docs now make the checkpoint stop boundary explicit: its job is to stabilize the current truth surface, keep existing runtime shortcuts visible as blocked debt, and stop rather than absorb carrier-schema, donor-collider, ownership, observer-gate, or baseline-softening work.
- `PR-0215` first implementation checkpoint is now active in code: `launcherChain3d.ts` no longer promotes route speed from `resolveLaunchSpeedFromCharge(...)`, `LauncherTravelRoute.ts` no longer applies the old in-route speed floor, route capture/seam transitions now preserve non-zero route-direction velocity instead of zeroing it, and `PhysicsWorld.launcher.behavior.spec-impl.ts` now locks planar-speed non-amplification plus non-zero chained-seam velocity. The focused proof stack is green with a longer truthful observation window in `launchTraceContract.ts`, but the canonical live parity gate is currently blocked on expected drift against the old baseline (`first_board_collision_step_drift_blocked`, `handoff_to_board_step_drift_blocked`, `peak_speed_drift_blocked`), so `PR-0215` remains in progress.
- `PR-0216` remains the design-and-review packet before any true physical-rail conversion proceeds, and the architect direction is now canonicalized in `docs/reference/ref-flunk-out-frenzy-physical-rail-architect-direction-2026-04-04.md`. Sequencing is corrected accordingly: active `EPIC-33` / `ST-33-01` now slot ahead of further cut-over work and break into planned `PR-0217` / `PR-0218` / `PR-0219` for carrier-role schema, launcher-world carrier compiler foundation, and observer/cut-over readiness gating.
- `PR-0220` is now the last planning/review checkpoint before `PR-0217` implementation: it creates a second offline-review packet grounded in the approved `EPIC-33` stack and asks for explicit advice on the remaining pre-implementation uncertainties (carrier schema shape, launcher-world ownership boundary, donor-to-collider strategy, Rapier stability policy, observer semantics, temporary correction policy, and future baseline-repin trigger).
- `PR-0220` is now complete: the offline reviewer answered the remaining pre-implementation questions, and that guidance is now folded into `PR-0217` / `PR-0218` / `PR-0219`, so `PR-0217` no longer waits on a separate decision packet.
- The lead architect's follow-on review guidance is now encoded directly into `PR-0217` / `PR-0218` / `PR-0219`: tagged `carriers[]` union plus compile-role/ownership invariants in `PR-0217`, collider-family and donor-ownership-matrix requirements in `PR-0218`, and raw-row-backed carrier-occupancy/progress/correction facts plus explicit cut-over blockers in `PR-0219`.
- `REV-EPIC-33` is now approved in `docs/backlog/reviews/review-epic-33-flunk-out-frenzy-physical-carrier-foundations-and-cutover-governance.md`, so `EPIC-33` is active and `ST-33-01` is the approved prerequisite before any further physical cut-over continuation resumes.
- `PR-0210` frontend refactor closure is now repaired locally: the Flunk-Out Frenzy shell split remains intact (`FlunkOutFrenzyView.vue` + `views/apps/components/Fof*.vue` + `views/apps/styles/fof-shell-*.css`), the donor/table split now lives under `game/table/donor/` and `game/table/spec/`, `compilePinballTable.spec.ts`, `GameRuntime.spec.ts`, and `PhysicsWorld.launcher.spec.ts` are now thin entrypoints over focused imported test modules, and the remaining oversized runtime files are decomposed through `game/render/PixiRendererEffects.ts`, `game/physics/world/PhysicsWorldPlanBuilder.ts`, and `game/physics/launcher/{LauncherWorldGeometry,LauncherSensors,LauncherTelemetrySnapshot}.ts`. The broken rail rename/import drift and split-test contract regressions from the partial refactor are fixed, the Flunk-Out Frenzy frontend tree is back under the 500-line ceiling, and the full Flunk-Out Frenzy frontend spec surface is green locally.
- `tableDefinitionTypes.ts` now enforces chained-vs-terminal route semantics (chained routes forbid `handoffVelocity`, terminal route requires it), and `compilePinballTable.ts` hard-fails seam/contract violations (unique tags, route min points, min-charge bounds, nextRoute existence, cycle-free graph, seam continuity `xy<=1/z<=1`, exactly one feed sensor and one exit sensor, terminal-only board handoff).
- Runtime seam behavior now aligns with architect invariants: `launcherChain3d.ts` performs physical plunger-contact release gating, bounded post-release route-capture window, and sw16-only gate emission on real exit transition; `PhysicsWorld.ts` uses launcher-chain ownership/handoff for launch/relaunch rather than a main-world release shortcut.
- Docs-as-code and skill governance are updated for this invariant set: `docs/backlog/prs/pr-0200-flunk-out-frenzy-launcher-release-path-and-donor-wall-face-representation.md` and `~/.codex/skills/pinball-board-authoring/SKILL.md` now explicitly pin strict seam continuity, endpoint-bridge seam authoring, and no helper/freehand seam geometry.
- `EPIC-31` is now proposed in `docs/backlog/epics/epic-31-flappy-birds-curated-app.md`, with the required pending review in `docs/backlog/reviews/review-epic-31-flappy-birds-curated-app.md`. The scope fixes `games.flappy_birds` as a real bespoke curated app from day one, keeps the browser-owned runtime plus backend-owned leaderboard split, and explicitly narrows highscores to a lightweight teacher-fun feature with simple server-owned leaderboard state.
- The older competitive-games planning family has now been rewritten around lightweight leaderboard support rather than heavyweight submission-promotion assumptions: `docs/adr/adr-0073-competitive-games-and-official-high-scores.md`, `docs/reference/ref-competitive-games-cross-cutting-programme.md`, `docs/reference/ref-curated-app-flunk-out-frenzy-architecture-and-foundational-code.md`, `docs/backlog/epics/epic-25-competitive-games-and-flunk-out-frenzy.md`, `docs/backlog/reviews/review-epic-25-competitive-games-and-flunk-out-frenzy.md`, `docs/backlog/stories/story-25-02-flunk-out-frenzy-local-runtime-vertical-slice.md`, `docs/backlog/stories/story-25-03-competitive-play-pending-score-submission-and-typed-leaderboards.md`, `docs/backlog/stories/story-25-04-competitive-play-leaderboard-hardening-and-ruleset-scoping.md`, and `docs/backlog/stories/story-25-05-flunk-out-frenzy-mechanics-port-foundation.md`. `ST-25-04` was renamed from the old promotion framing to the new leaderboard-hardening/ruleset-scoping framing, and `docs/index.md` now points at the renamed story.
- Public curated-app guest-access planning is now approved in `docs/adr/adr-0079-public-curated-app-access-profiles-and-guest-state-boundaries.md`, `docs/backlog/epics/epic-32-public-curated-app-access-foundation-and-klassrumskartan-demo.md`, `docs/backlog/stories/story-32-01-curated-app-public-access-profiles-and-current-app-matrix.md` through `story-32-06-klassrumskartan-demo-adoption-on-the-public-browser-workspace-profile.md`, and `docs/backlog/reviews/review-epic-32-public-curated-app-access-foundation-and-klassrumskartan-demo.md`; the approved package keeps authenticated `/apps/:appId` and `/api/v1/apps/...` seams unchanged, locks the registry as the public-profile source of truth, makes public helpers cookie-agnostic, makes snapshot/import conflict semantics testable, and treats smart rules as first-class guest scope for the first `public_browser_workspace_with_upgrade` consumer.
- `EPIC-32` foundation slice `ST-32-01` + `ST-32-02` is now implemented locally: curated-app definitions now carry canonical `public_access_profile` metadata in `src/skriptoteket/domain/curated_apps/models.py` and `src/skriptoteket/infrastructure/curated_apps/registry.py`; the parallel public bootstrap seam lives in `src/skriptoteket/web/api/v1/public_apps.py`; the dedicated public host route lives at `frontend/apps/skriptoteket/src/router/routes.ts` -> `frontend/apps/skriptoteket/src/views/PublicAppHostView.vue`; and Klassrumskartan now has an app-specific public entry shell in `frontend/apps/skriptoteket/src/views/apps/ClassroomPlannerEntryView.vue` without weakening authenticated `/apps/:appId` or `GET /api/v1/apps/{app_id}`. The local Playwright taxonomy cleanup is also in place: canonical browser gates are now `scripts/playwright_ui_smoke.py`, `scripts/playwright_ui_editor_smoke.py`, `scripts/playwright_ui_runtime_smoke.py`, `scripts/playwright_classroom_planner_smoke.py`, and `scripts/classroom_planner_draft_kind_api_smoke.py`; shared helpers were extracted into `scripts/_playwright_browser.py`, `scripts/_playwright_auth.py`, and `scripts/_playwright_classroom_planner.py`; and the retired targeted proofs `scripts/playwright_pr_0105_seating_continuity.py`, `scripts/playwright_pr_0112_design_transition_check.py`, and `scripts/playwright_pr_0113_reset_current_draft.py` were removed after taxonomy/pruning review.
- `PR-0221` has now been tightened in planning after reviewer feedback: the authenticated guest-upgrade slice must server-recompute `snapshot_content_hash` and entity fingerprints before dedupe/import decisions, must gate authenticated planner bootstrap behind the upgrade prompt, and now explicitly includes one narrow durable draft import identity / lookup seam so repeated guest-import commits do not duplicate historical drafts.
- The independent `skriptoteket_reviewer` pass for the `EPIC-32` foundation slice plus taxonomy cleanup returned no actionable findings; residual note only: decide explicitly later whether future public profiles stay bespoke-only or need a generic public fallback, and keep mapping any future proof pruning back to canonical or unit coverage. No `skriptoteket_implementation_specialist` fix loop was needed for this pass.
- `ST-32-04` `Option A1` contract + storage foundation is now implemented locally and anchored in `docs/backlog/prs/pr-0211-st-32-04-option-a1-guest-snapshot-frontend-hardening.md`: the guest snapshot contract/storage seam lives in `frontend/apps/skriptoteket/src/views/apps/classroomPlannerGuestSnapshot.ts`, `classroomPlannerGuestStorage.ts`, and `classroomPlannerGuestSnapshotMapping.ts`; the public-shell status controller lives in `useClassroomPlannerGuestSnapshotStatus.ts`; and the public Klassrumskartan shell in `ClassroomPlannerEntryView.vue` now reflects browser-owned guest snapshot state without weakening the authenticated host. Review-remediation follow-up is also applied locally: guest storage is lazy so authenticated host setup does not bind browser-owned storage, guest digests are now deterministic `sha256:<64 hex>` values, entry-shell tests now cover loading/error/expired states, and storage tests now cover orphaned-pointer repair.
- `PR-0221` is now partially hardened locally after review findings: focused guest-upgrade backend collaborators replace the old monolith, the authenticated `/api/v1/apps/classroom.group-seating-studio/guest-upgrade` route plus DI seam are wired, draft persistence now carries `task_entry_classroom_selection_mode` + `guest_import_identity` via migration `d3a9f6b2c4e7`, the authenticated Klassrumskartan host blocks planner bootstrap behind the import/postpone/discard prompt, guest-upgrade now reuses shared workspace/smart-rule validators, malformed smart rules and invalid imported drafts return structured `conflicted` receipts instead of partial saves, metadata-only checkpoint compatibility support is removed so checkpoint payloads are now required by contract, the frontend guest snapshot contract already carries importable checkpoint payload fields instead of label-only descriptors, and the repeat-commit idempotency proof now lives in `tests/unit/application/apps/classroom_planner/test_guest_upgrade_idempotency.py`.
- `ST-25-05` / `PR-0190` are now implemented locally: `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/rules/RuleEngine.ts` is now an orchestrator over the new `ruleTypes.ts`, `scoreState.ts`, `bonusJackpotState.ts`, and `ballLifecycleState.ts` helpers; the runtime seam now carries nested `bonus`, `jackpot`, and `ballLifecycle` HUD state through `runtimeEngineTypes.ts`, `runtimeTypes.ts`, `GameRuntime.ts`, `GameHost.vue`, and `FlunkOutFrenzyView.vue`; `PrototypeAlphaGameEngine.ts`, `gameEffectTypes.ts`, `PixiRenderer.ts`, and `AudioDirector.ts` now surface semantic bonus/jackpot/shoot-again effects; and the post-review rollover exploit fix now gates rollover bonus accrual to newly lit lanes only, with a regression added in `RuleEngine.spec.ts`.
- The `PR-0190` review loop is now documented: the preflight `skriptoteket_reviewer` pass raised no actionable blockers on the planned split, the first post-implementation review found the duplicate-lit-rollover bonus exploit, `skriptoteket_implementation_specialist` fixed that narrow slice plus the regression test, and the final reviewer verdict for the post-fix diff is clean from the lead-review pass in this session.
- `ST-25-05` / `PR-0189` are now implemented locally: `tableDefinitionTypes.ts` owns the shared Flunk-Out Frenzy authored geometry contracts, `prototypeAlphaTable.ts` now includes grouped rollover metadata plus the first tripwire, gate, standup-target, and popup-target definitions, `createLaneDevices.ts` and `createTargetDevices.ts` build those authored devices into Rapier sensors/colliders, and the new events are surfaced through `PrototypeAlphaGameEngine.ts`, `gameEffectTypes.ts`, `PixiRenderer.ts`, and `AudioDirector.ts` so the device slice has end-to-end feedback without pulling rule complexity forward.
- `ST-25-05` / `PR-0188` are now implemented locally: `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/physicsTypes.ts` now exposes the widened future-facing `MachineEvent` union, `colliderMeta.ts` and `machineEventEmitter.ts` own collider semantics and Rapier contact translation, and `PhysicsWorld.ts` is reduced to 420 lines so the world class is back to orchestration rather than inline event plumbing.
- `EPIC-25` now includes the next local gameplay-planning slice: `docs/backlog/stories/story-25-05-flunk-out-frenzy-mechanics-port-foundation.md` defines the post-vertical-slice mechanics-port foundation, anchored to the local `.artifacts/SpaceCadetPinball` donor analysis, and now treats `PR-0188` through `PR-0190` as the first gated tranche, `PR-0191` as the formal reassessment and go/no-go checkpoint, and `PR-0192` through `PR-0195` as the higher-risk physical-fidelity and advanced-device follow-on work.
- `ST-29-06` / `PR-0187` are now implemented locally: `frontend/apps/skriptoteket/src/views/apps/classroomPlannerPayloadNormalization.ts`, `classroomPlannerStateSupport.ts`, `classroomPlannerLifecycle.ts`, `useClassroomPlannerRouteShell.ts`, and `classroomPlannerRouteShellErrors.ts` keep the planner boundary hardening for thin no-classroom payloads and suppress raw runtime/framework text; the canonical local `5173` repro is now known to use the backend-served static SPA bundle until `pdm run fe-build` realigns it; `frontend/apps/skriptoteket/src/views/apps/components/PlannerRulesMapCanvas.vue` now renders the approved no-classroom empty-state copy in default `Planeringsvy` while keeping the planning eyebrow on the class name and reserving `Ej på karta` for `Klassrumsvy`; `scripts/playwright_pr_0185_rules_no_classroom_fallback_check.py` now proves that live header contract; and `.codex/agents/skriptoteket-reviewer.toml` now explicitly states that `skriptoteket_reviewer` is the sole reviewer and must not delegate.
- The sole-reviewer loop is now closed for `PR-0187`: the first `skriptoteket_reviewer` pass found a stale draft-save acknowledgement normalization gap in `frontend/apps/skriptoteket/src/views/apps/classroomPlannerStateSupport.ts`, `skriptoteket_implementation_specialist` fixed that slice plus added coverage in `classroomPlannerStateSupport.spec.ts`, and the final sole-reviewer rerun returned no actionable findings.
- `main` and Hemma are now aligned through the post-`PR-0187` deploy: `ssh hemma 'cd ~/apps/skriptoteket && ./scripts/hemma_deploy_and_verify_seating_export.sh'` completed successfully, and stale dangling Docker images were pruned afterward on Hemma.
- Backlog reconciliation is now current for `EPIC-29`: `ST-29-01`, `ST-29-04`, `ST-29-05`, and `ST-29-10` are now written as done to match the shipped desktop-first planner behavior; `PR-0127`, `PR-0131`, `PR-0132`, `PR-0156`, `PR-0182`, `PR-0183`, and `PR-0184` are marked done; `PR-0158` is canceled as stale seating-first planning; and the remaining follow-on primitive/symbol lane is now isolated in `ST-29-11` and `ST-29-12`, with `ST-29-08` positioned as the later custom-tooltip enhancement on top of that baseline.
- `ST-29-11` now has an explicit three-slice execution path: `PR-0195` for shared primitive-contract normalization plus generic menu/split behavior, `PR-0196` for planner wrapper thinning and action-surface adapter cleanup, and `PR-0197` for editor/site adoption proof plus segmented-toggle contract completion.
## Verification
- 2026-04-03 `EPIC-32` public curated-app foundation + Playwright taxonomy cleanup:
  - `pdm run docs-validate` (pass)
  - `pdm run fe-gen-api-types` (pass)
  - `pdm run pytest tests/unit/infrastructure/curated_apps/test_registry.py tests/unit/web/test_apps_api_routes.py tests/unit/web/test_public_apps_api_routes.py` (pass; `8 passed`)
  - `pdm run fe-test -- --run src/router/index.spec.ts src/views/AppHostView.spec.ts src/views/PublicAppHostView.spec.ts` (pass)
  - `pdm run fe-type-check` (pass)
  - `pdm run fe-build` (pass)
  - `pdm run python -m scripts.playwright_classroom_planner_smoke --base-url http://127.0.0.1:5173` (initially failed on stale grouping selector/close-path assumptions; repaired to current Smart-settings + Regler flow, then passed)
  - `pdm run python -m py_compile scripts/_playwright_browser.py scripts/_playwright_auth.py scripts/_playwright_classroom_planner.py scripts/playwright_ui_smoke.py scripts/playwright_ui_editor_smoke.py scripts/playwright_ui_runtime_smoke.py scripts/playwright_suggestion_new_smoke.py scripts/playwright_classroom_planner_smoke.py scripts/classroom_planner_draft_kind_api_smoke.py scripts/playwright_pr_0111_overview_resumable_entry.py scripts/playwright_pr_0116_room_template_editor_check.py scripts/playwright_pr_0120_seating_export_check.py scripts/playwright_pr_0136_room_editor_toggle_check.py scripts/playwright_pr_0137_class_list_import_check.py scripts/playwright_pr_0154_smart_seating_check.py scripts/playwright_pr_0155_rules_workspace_check.py scripts/playwright_pr_0157_dense_toolbar_check.py scripts/playwright_pr_0157_group_card_alignment_check.py scripts/playwright_pr_0157_overview_alignment_check.py scripts/playwright_pr_0165_seating_editor_sync_check.py scripts/playwright_pr_0166_topbar_brand_hit_target_check.py scripts/playwright_pr_0166_transition_continuity_audit.py scripts/playwright_pr_0167_smart_grouping_cutover_check.py scripts/playwright_pr_0175_class_list_import_dropzone_check.py scripts/playwright_pr_0176_auth_recovery_check.py scripts/playwright_pr_0177_rule_visibility_and_tool_feedback_check.py scripts/playwright_pr_0179_sticky_toolbar_offset_check.py scripts/playwright_pr_0181_smart_settings_copy_check.py scripts/playwright_pr_0185_rules_no_classroom_fallback_check.py scripts/playwright_flunk_out_frenzy_route_check.py scripts/playwright_hmr_probe.py scripts/playwright_nav_transitions_smoke.py scripts/playwright_seating_drag_preview_check.py scripts/playwright_vault_sort_subrail_check.py` (pass)
  - `pdm run python -m scripts.playwright_classroom_planner_smoke --base-url http://127.0.0.1:5173` (final post-prune pass; pass; artifacts in `.artifacts/classroom-planner-smoke`)
  - `pdm run docs-validate` (pass after deleting retired targeted proofs and updating the `PR-0105` docs breadcrumb)
  - live browser checks:
    - public route `http://127.0.0.1:5173/public/apps/classroom.group-seating-studio` renders the public Klassrumskartan host shell; `Logga in till full version` opens the login modal; no router warning remains after replacing the fake `/login` link with the modal call
    - authenticated route `http://127.0.0.1:5173/apps/classroom.group-seating-studio` still renders the full planner shell; session-backed route proof confirmed `Klassrumskartan` plus `[data-test="overview-roster-panel"]`
  - independent reviewer: `skriptoteket_reviewer` (`Beauvoir`) returned no actionable findings; no `skriptoteket_implementation_specialist` iteration was required
- 2026-04-04 `ST-32-04` guest snapshot contract + storage foundation and review remediation:
  - `docs/backlog/prs/pr-0211-st-32-04-option-a1-guest-snapshot-frontend-hardening.md` now captures the missing reviewed Option A1 remediation slice and `docs/index.md` links it
  - `pdm run fe-test -- --run src/views/apps/ClassroomPlannerEntryView.spec.ts src/views/apps/classroomPlannerGuestStorage.spec.ts src/views/apps/classroomPlannerGuestSnapshotMapping.spec.ts src/views/apps/classroomPlannerGuestFingerprint.spec.ts src/views/apps/useClassroomPlannerGuestSnapshotStatus.spec.ts` (pass; `21 passed`)
  - `pdm run docs-validate` (pass)
  - `pdm run fe-type-check` (blocked by unrelated local Flunk-Out Frenzy spec import in `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts`: `Cannot find module 'node:fs/promises'`; do not fold that unrelated lane into `ST-32-04`)
  - live browser proof on `http://127.0.0.1:5173/public/apps/classroom.group-seating-studio` via Playwright MCP: the public Klassrumskartan shell rendered successfully and showed the missing guest-workspace state with the `Initiera lokal gästarbetsyta` CTA
  - independent reviewer re-run (`skriptoteket_reviewer`, `Hubble`) returned no actionable findings after remediation; remaining explicit non-blockers are the unrelated local Flunk-Out Frenzy `fe-type-check` failure and that `task_entry_classroom_selection_mode` is preserved in the guest snapshot contract but still not consumed by hydration and should be resolved in the later authenticated import slice
- 2026-04-04 `PR-0221` authenticated guest-upgrade orchestration + prompt gate:
  - `pdm run pytest tests/unit/application/apps/classroom_planner/test_guest_upgrade_idempotency.py tests/unit/application/apps/classroom_planner/test_guest_upgrade_handler.py tests/unit/web/apps/classroom_planner/test_guest_upgrade_api.py` (pass; `7 passed`), `pnpm --filter @skriptoteket/spa exec vitest run src/views/apps/classroomPlannerGuestSnapshotMapping.spec.ts src/views/apps/useClassroomPlannerGuestUpgrade.spec.ts src/views/apps/ClassroomPlannerEntryView.spec.ts` (pass; `15 passed`), and `pdm run docs-validate` (pass)
  - `pdm run fe-type-check` remains blocked only by the unrelated local Flunk-Out Frenzy file `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/launcher/LauncherTravelRoute.ts` (`segmentStartDistance` missing); the `PR-0221` checkpoint-contract patch introduced no remaining type errors after adding the missing guest snapshot mapping imports
  - local live sanity checks on the running dev stack: `curl -I http://127.0.0.1:5173/public/apps/classroom.group-seating-studio` returned `200 OK`, `curl -s http://127.0.0.1:8000/healthz` returned healthy JSON, and Playwright MCP reached `http://127.0.0.1:5173/public/apps/classroom.group-seating-studio` and rendered the shell/loader state (`Skriptoteket`, `Laddar...`); keep the stronger earlier authenticated prompt proof above as the canonical end-to-end guest-upgrade browser verification for this slice
- 2026-04-03 `ST-25-06` / `PR-0200` endpoint-bridge + strict seam contracts + physical-release closure:
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/plungerLaneState.spec.ts src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts` (pass; 34/34 tests)
  - `pdm run fe-type-check` (pass)
  - `pdm run fe-build` (pass)
  - `pdm run docs-validate` (pass)
  - `pdm run python -m scripts.playwright_flunk_out_frenzy_route_check --base-url http://127.0.0.1:5173` -> `playwright-flunk-out-frenzy: ok -> .artifacts/flunk-out-frenzy-route-check`
  - user-owned live browser check: plunger does not move and has no visible effect on the pinball; launcher closure not functionally accepted yet.
- 2026-04-03 `ST-25-06` / `PR-0206` proof-first launcher root-cause lane scaffolding:
  - added `docs/backlog/prs/pr-0206-flunk-out-frenzy-plunger-strike-root-cause-proof-and-telemetry-contract.md`
  - independent reviewer verdict: approved
- 2026-04-03 `ST-25-06` / `PR-0206` proof-lane code implementation:
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts src/components/apps/flunk-out-frenzy/game/input/KeyboardInputController.spec.ts` (expected: 1 red proof test in `PhysicsWorld.spec.ts`; 47 passing)
  - `pdm run fe-type-check` (pass)
  - live check on `http://127.0.0.1:5173/apps/games.flunk_out_frenzy`: started runtime, confirmed `window.__FOF_DEBUG__.launcherTelemetry()` exists and returns `input` + `launcher.{plunger,ball,route,routeCapture,sensors,contact}` fields
- 2026-04-03 `ST-25-06` / `PR-0207` proof-first gate implementation updates (tests + telemetry contract only):
  - independent ruthless review (`Feynman`) re-run on updated `docs/backlog/prs/pr-0207-flunk-out-frenzy-launcher-strike-ready-rest-pose-alignment.md` returned `approved`
  - implemented proof-contract updates only in:
    - `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts`
    - `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts`
    - `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts`
  - no production geometry/runtime behavior files were edited in this PR-0207 pass
- 2026-04-03 `ST-25-06` / `PR-0208` strike-ready rest pose + release-contact integration implementation:
  - added `docs/backlog/prs/pr-0208-flunk-out-frenzy-strike-ready-rest-pose-and-release-integration-fix.md`
  - updated `docs/index.md`
  - implemented bounded launcher runtime fix in `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/launcherChain3d.ts`:
    - strike-ready rest-position alignment against plunger front face (`restGapPx=1`)
    - bounded post-release substep integration window (`4ms` substeps over `64ms`) for contact determinism
    - contact telemetry now aggregates overlap/relative-contact velocity across substeps
  - tightened proof typing guard in `frontend/apps/skriptoteket/src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts` for non-null rest separation telemetry
  - focused verification status after first implementation pass:
    - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts src/components/apps/flunk-out-frenzy/game/input/KeyboardInputController.spec.ts` (red; 5 failing `PhysicsWorld.spec.ts` cases)
    - `pdm run fe-type-check` (pass)
    - `pdm run fe-build` (pass)
    - `pdm run docs-validate` (pass)
  - independent ruthless reviewer (`Feynman`) verdict on PR-0208 pass: `changes_requested` (direction correct but release seam still incomplete; handoff evidence needed)
  - minimal corrective release-seam patch attempt added `RELEASE_STRIKE_LEAD_PX` during bounded release window in `launcherChain3d.ts`
  - focused verification status after corrective attempt:
    - same `fe-test` command remains red (same 5 failing `PhysicsWorld.spec.ts` cases)
    - `pdm run fe-type-check` (pass)
    - `pdm run fe-build` (pass)
    - `pdm run docs-validate` (pass)
  - second minimal corrective release-seam patch added dynamic release-window duration derived from plunger travel-to-strike distance and contact-aware release strike target in `launcherChain3d.ts`
  - focused verification status after second corrective attempt:
    - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts src/components/apps/flunk-out-frenzy/game/input/KeyboardInputController.spec.ts` (pass; 51/51 tests)
    - `pdm run fe-type-check` (pass)
    - `pdm run fe-build` (pass)
    - `pdm run docs-validate` (pass)
  - required live launcher check:
    - `pdm run python -m scripts.playwright_flunk_out_frenzy_route_check --base-url http://127.0.0.1:5173` -> `playwright-flunk-out-frenzy: ok -> .artifacts/flunk-out-frenzy-route-check`
- 2026-04-04 `ST-25-06` / `PR-0209` proof-lane reconciliation + focused verification rerun:
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/plungerLaneState.spec.ts src/components/apps/flunk-out-frenzy/game/physics/PhysicsWorld.spec.ts` (pass; `39/39`)
  - `pdm run fe-type-check` (pass)
  - `pdm run fe-build` (pass)
  - `pdm run docs-validate` (pass)
  - proof artifact persistence verified at `frontend/apps/skriptoteket/.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json` (includes `K-RELAUNCH-MEDIUM` with `sw16_exit_observed=true` and no invariant violations).
  - live launcher trace script run (`pdm run python -m scripts.playwright_flunk_out_frenzy_launch_trace_check --base-url http://127.0.0.1:5173 --artifact-dir .artifacts/flunk-out-frenzy-launch-to-drop`) now passes end-to-end after navigation-safe predicate stepping hardening; output: `playwright-flunk-out-frenzy-launch-trace: ok -> .artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`
- 2026-04-04 `PR-0213` closure + `PR-0214` operator summary/gate implementation:
  - `pdm run pytest tests/unit/scripts/test_flunk_out_frenzy_launch_trace_summary.py` (pass; `4 passed`)
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.launcher.proof.spec-impl.ts` (pass; `2 passed`)
  - `pdm run ruff check scripts/_flunk_out_frenzy_launch_trace_summary.py scripts/_playwright_flunk_out_frenzy_launch_trace_parity.py scripts/playwright_flunk_out_frenzy_launch_trace_parity_check.py scripts/playwright_flunk_out_frenzy_launch_trace_check.py tests/unit/scripts/test_flunk_out_frenzy_launch_trace_summary.py` (pass)
  - `pdm run python -m scripts.playwright_flunk_out_frenzy_launch_trace_parity_check --base-url http://127.0.0.1:5173 --artifact-dir .artifacts/flunk-out-frenzy-launch-to-drop` (pass; emits raw trace matrix + summary JSON + summary Markdown)
  - `pdm run docs-validate` (pass)
  - canonical operator artifacts:
    - raw: `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`
    - machine summary: `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.json`
    - human summary: `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.md`
- 2026-04-04 `PR-0215` runtime-remediation checkpoint 1:
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.launcher.spec.ts src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/plungerLaneState.spec.ts` (pass; `33/33`)
  - `pdm run fe-type-check` (pass)
  - `pdm run fe-build` (pass)
  - `pdm run docs-validate` (pass)
  - `pdm run python -m scripts.playwright_flunk_out_frenzy_launch_trace_parity_check --base-url http://127.0.0.1:5173 --artifact-dir .artifacts/flunk-out-frenzy-launch-to-drop` (blocked by PR-0214 summary gate drift: `first_board_collision_step_drift_blocked`, `handoff_to_board_step_drift_blocked`, `peak_speed_drift_blocked`)
  - current launcher artifacts to inspect after the blocked live run:
    - `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`
    - `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.json`
    - `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.md`
    - `frontend/apps/skriptoteket/.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`
- 2026-04-04 `PR-0210` file-size compliance + frontend decomposition repair:
  - `pdm run fe-type-check` (pass)
  - focused refactor-regression rerun: `pdm run fe-test -- --run src/views/apps/FlunkOutFrenzyView.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.collisions.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.flippers.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.captureDevices.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.launcher.spec.ts src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts` (pass; `56/56`)
  - full Flunk-Out Frenzy frontend suite rerun: `pnpm test -- --run src/components/apps/flunk-out-frenzy/game/engine/PrototypeAlphaGameEngine.spec.ts src/views/apps/FlunkOutFrenzyView.spec.ts src/components/apps/flunk-out-frenzy/game/physics/plungerLaneState.spec.ts src/components/apps/flunk-out-frenzy/game/physics/captureDeviceLifecycle.spec.ts src/components/apps/flunk-out-frenzy/game/physics/flipperContactModel.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.flippers.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.captureDevices.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.collisions.spec.ts src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.launcher.spec.ts src/components/apps/flunk-out-frenzy/game/input/KeyboardInputController.spec.ts src/components/apps/flunk-out-frenzy/game/rules/bonusJackpotState.spec.ts src/components/apps/flunk-out-frenzy/game/rules/ballLifecycleState.spec.ts src/components/apps/flunk-out-frenzy/game/rules/scoreState.spec.ts src/components/apps/flunk-out-frenzy/game/rules/captureAwardsState.spec.ts src/components/apps/flunk-out-frenzy/game/rules/RuleEngine.spec.ts src/components/apps/flunk-out-frenzy/game/core/CommandQueue.spec.ts src/components/apps/flunk-out-frenzy/game/core/FixedStepRunner.spec.ts src/components/apps/flunk-out-frenzy/game/core/GameRuntime.spec.ts src/components/apps/flunk-out-frenzy/GameHost.spec.ts src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/audio/AudioDirector.spec.ts` (pass; `106/106`)
  - live route proof: `curl -I http://127.0.0.1:5173/apps/games.flunk_out_frenzy` returned `200 OK`; `curl -I http://127.0.0.1:8000/api/v1/apps/games.flunk_out_frenzy/bootstrap` returned `405 Method Not Allowed` with `allow: GET`, confirming the backend route is live; `pdm run python -m scripts.playwright_flunk_out_frenzy_route_check --base-url http://127.0.0.1:5173` returned `playwright-flunk-out-frenzy: ok -> .artifacts/flunk-out-frenzy-route-check`
  - manual-only checklist note: the explicit Chrome DevTools 30s 60fps audit and heap-snapshot memory delta check from `docs/backlog/prs/pr-0210-flunk-out-frenzy-file-size-compliance-and-frontend-module-decomposition.md` were not rerun in this session; automated route/runtime checks passed, but that perf/memory sign-off still needs a human browser pass if strict checklist closure is required.
## How to Run
```bash
ARTIFACTS_ROOT=/tmp/skriptoteket/artifacts pdm run dev-local
pdm run python -m scripts.playwright_classroom_planner_smoke --base-url http://127.0.0.1:5173
ssh hemma 'cd ~/apps/skriptoteket && ./scripts/hemma_deploy_and_verify_seating_export.sh'
```
## Known Issues / Risks
- Launcher telemetry proof is now canonicalized through the PR-0214 operator surface (raw matrix + summary gate), but user-owned manual live checks still report inert plunger/ball interaction that needs direct visual reconciliation against the otherwise-green trace outputs.
- The canonical local browser proof for Klassrumskartan no-classroom flows depends on the backend-served static SPA bundle once auth redirects into `:8000`; rerun `pdm run fe-build` before trusting `http://127.0.0.1:5173` Playwright outcomes after frontend changes.
- The warnings-as-errors audit still surfaces a dependency-level Python 3.14 deprecation in `pytest-asyncio` (`asyncio.AbstractEventLoopPolicy`); repo-owned code is clean for the audited patterns, but the plugin likely needs a compatible bump.
- Keep the `7d4c1a2b9e6f` repair migration in mind if a long-lived local DB reports Alembic head but misses the roster smart-rule root contract.
- `tests/integration/test_migration_revision_coverage_idempotent.py` still has one pre-existing full-suite harness failure on merge revision `6a1e9d3c4b7f` because `tests/integration/migration_idempotency_support.py` assumes a linear `down_revision`; the new `b7f9c2d4e1a6` revision itself passes both targeted migration checks.
## Next Steps
- Continue `EPIC-32` from the current no-legacy posture: keep browser-authored guest snapshots on the canonical checkpoint payload contract and do not reintroduce metadata-only checkpoint fallback support without a new explicit docs-as-code decision.
- Use `pdm run python -m scripts.playwright_flunk_out_frenzy_launch_trace_parity_check --base-url http://127.0.0.1:5173 --artifact-dir .artifacts/flunk-out-frenzy-launch-to-drop` plus `launch-to-drop-trace-summary.md` as the canonical launcher status/debug/decision surface before any further launcher behavior changes.
- Start `PR-0215` from `docs/backlog/prs/pr-0215-flunk-out-frenzy-launcher-runtime-shortcut-remediation-and-physical-truth-alignment.md`; keep the runtime work proof-first by rerunning exactly:
  - `pdm run fe-test -- --run src/components/apps/flunk-out-frenzy/game/physics/__tests__/PhysicsWorld.launcher.spec.ts src/components/apps/flunk-out-frenzy/game/table/compilePinballTable.spec.ts src/components/apps/flunk-out-frenzy/game/physics/plungerLaneState.spec.ts`
  - `pdm run fe-type-check`
  - `pdm run fe-build`
  - `pdm run docs-validate`
  - `pdm run python -m scripts.playwright_flunk_out_frenzy_launch_trace_parity_check --base-url http://127.0.0.1:5173 --artifact-dir .artifacts/flunk-out-frenzy-launch-to-drop`
  - review artifacts at `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`, `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.json`, `.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-summary.md`, and `frontend/apps/skriptoteket/.artifacts/flunk-out-frenzy-launch-to-drop/launch-to-drop-trace-matrix.json`
  - run and record one blocking manual launcher matrix pass for `rest`, `short`, `medium`, `full`, and `relaunch`; do not accept `PR-0215` while manual live behavior still contradicts the automated trace
  - if the checkpoint only proves the remaining shortcut debt honestly, stop there and route any carrier-schema, donor-collider, ownership, observer-gate, or baseline-softening asks into `EPIC-33` / `ST-33-01` instead of extending `PR-0215`
- Do not continue physical cut-over inside `PR-0215`. Move first through active `EPIC-33` / `ST-33-01`, start `PR-0217` with the now-encoded architect/offline-review guidance, then continue `PR-0218` / `PR-0219`.
- Run final independent ruthless review sign-off for `PR-0208` with the now-green focused verification set and recorded automated live launcher route check.
- Perform one final human visual launcher matrix sanity check (rest/short/medium/full/relaunch) before marking donor-launcher closure fully accepted.
- Review `EPIC-31` / `REV-EPIC-31`, then scaffold the first `ST-31-0x` story stack for `games.flappy_birds` around substrate/bootstrap, local playable runtime, and lightweight leaderboard plumbing.
- If the donor corrective lane is accepted, move `PR-0193` from local implementation state to reviewed/merged status and then continue with `PR-0194`/`PR-0195`.
- Next planning choice inside `EPIC-29` is to implement `ST-29-11` in order through `PR-0195`, `PR-0196`, and `PR-0197`, then define the `ST-29-12` symbol/discoverability slice sequence before taking `ST-29-08`.
- If the no-classroom proof is rerun after more frontend changes, rebuild the backend-served SPA first with `pdm run fe-build`.
