"""OpenAI-compatible provider implementations.

This package contains the OpenAI-compatible HTTP providers for:
- inline completion
- chat streaming
- chat-ops (JSON edit operations)

The public classes are re-exported via `skriptoteket.infrastructure.llm.openai_provider`
to keep import paths stable.
"""
