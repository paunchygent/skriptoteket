version: '3.8'

services:
  web:
    build: .
    container_name: utility_scripts
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - ENVIRONMENT=development
    restart: unless-stopped
